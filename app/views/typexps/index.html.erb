<h1>Listing typexps</h1>

<table>
  <tr>
  </tr>

<% for typexp in @typexps %>
  <tr>
    <td><%= link_to 'Show', typexp %></td>
    <td><%= link_to 'Edit', edit_typexp_path(typexp) %></td>
    <td><%= link_to 'Destroy', typexp, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New typexp', new_typexp_path %>
